# App
PORT=3000
NODE_ENV=development

# Database (set automatically in docker-compose for containers)
DATABASE_URL=postgresql://ceo:ceo_secret@localhost:5432/cold_email_os

# Redis (set automatically in docker-compose for containers)
REDIS_URL=redis://localhost:6379

# AI — Claude (Anthropic)
ANTHROPIC_API_KEY=sk-ant-...

# External APIs
APIFY_API_TOKEN=apify_api_...
JINA_API_KEY=                 # optional — Jina works without auth at lower rate limits
